import { useEffect } from "react";

const AdminDashboard =() => {

    useEffect(() => {
    },[]);

    window.document.title = "My Dashboard";

    return(
            <div className="">
                <p>Admin dashboard</p>
            </div>
        );
};

export default AdminDashboard;